<h2>Home Page</h2>
<%= link_to "Add a link", new_link_path %>

<ul>
  <% @links.each do |link| %>
  <p><strong>Title: </strong><%=link_to link.title,  "http://#{link.url}" %></p>
  <p> <strong>Url: </strong><%= link.url %></p></br>
  <% end %>
</ul>

<%= will_paginate @links %>
